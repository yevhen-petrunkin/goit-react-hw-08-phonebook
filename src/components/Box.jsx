import styled from 'styled-components';
import {
  color,
  space,
  typography,
  layout,
  flexbox,
  border,
  shadow,
} from 'styled-system';

export const Box = styled.div`
   {
    box-sizing: border-box;
  }
  ${color}
  ${space}
  ${typography}
  ${layout}
  ${flexbox}
  ${border}
  ${shadow}
`;
